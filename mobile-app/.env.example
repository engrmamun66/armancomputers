# Copy to .env and adjust per run target, then pass via:
#   flutter run --dart-define-from-file=.env
#   flutter build apk --dart-define-from-file=.env

# Android emulator (10.0.2.2 = host machine's localhost from inside the emulator)
API_BASE_URL=http://10.0.2.2:8000/api
API_ORIGIN=http://10.0.2.2:8000

# Chrome:              http://127.0.0.1:8000/api        http://127.0.0.1:8000
# Real device (LAN IP): http://<your-lan-ip>:8000/api    http://<your-lan-ip>:8000
